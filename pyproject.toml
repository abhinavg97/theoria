[project]
name = "theoria"
version = "0.1.0"
description = "Verified reasoning — make an LLM prove its answer, then have judges check every step"
license = "Apache-2.0"
requires-python = ">=3.12"
dependencies = [
    "pyyaml>=6.0",
    "datasets>=4.0",
    "jsonschema>=4.21",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
]
test = [
    "pytest>=8.0",
]

[project.scripts]
theoria = "cli:main"

[build-system]
requires = ["setuptools==80.9.0"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
py-modules = [
    "pipeline",
    "llm",
    "harness",
    "sandbox",
    "loaders",
    "grade",
    "observability",
    "telemetry",
    "export_problem",
    "cli",
    "agent_loop",
]
