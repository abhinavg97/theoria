# Unaudited answer-grader profile using Codex's built-in Ollama adapter.
# Override the example model with `theoria grade --codex-model MODEL`.

audit_grader:
  backend: codex
  model: gpt-oss:20b
  effort: null
  oss: true
  local_provider: ollama
  oss_base_url: http://localhost:11434/v1
  provider_env: []
  search: false
  schema_retries: 1
